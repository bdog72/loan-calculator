{
  "private": true,
  "scripts": {
    "deploy": "surge ./public --domain loan-calculator.brianbycynski.surge.sh",
    "start": "browser-sync start --server 'public' --files 'public'"
  },
  "dependencies": {
    "browser-sync": "^2.23.3",
    "stylelint": "^8.4.0",
    "stylelint-config-standard": "^18.0.0",
    "surge": "^0.19.0"
  }
}
